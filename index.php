<?php 

    require_once("config.php");

    // retorna um usuário
    // $root = new Usuario();

    // $root->loadbyId(4);

    // if(!is_null($root)){
    //     echo $root;
    // };

    //retorna uma lista
    // $lista = Usuario::getList();

    // echo json_encode($lista);

    //faz uma busca por usuários
    // $users = Usuario::search('a  ');

    // echo json_encode($users);

    //busca um usuário
    // $user = new Usuario();

    // $user->login('leticia', 'joao');

    // echo $user;

    //Cria um novo usuário
    // $aluno = new Usuario("Valter", "!@#$");

    // $aluno->insert($aluno->getDeslogin(), $aluno->getDessenha());

    // //echo $aluno;

    //Atualiza um usuario
    // $user = new Usuario();

    // $user->loadById(3);

    // $user->update("Aleatorio", "@#!@#$", 3);

    // echo $user;

    //Deleta um usuario

    // $user = new Usuario();

    // $user->loadById(3);

    // $user->delete();

    // echo $user;

?>